# AGENTS.md

本文件是 Codex 在本工程中的工作规约。后续重新打开项目时，应优先读取并遵守本文件。

## 项目定位

本工程用于系统学习 AI Agent。核心材料在 `source/` 目录下，共 9 份教程。学习目标不是简单摘抄教程，而是把所有教程合并成一套可执行的 Agent 工程学习路线，并逐步实现一个综合 Agent。

## 重要文件

- `README.md`：项目入口。
- `docs/agent-learning-guide.md`：唯一学习指南，维护学习方法、五阶段路线、实践产物和完成标准。
- `docs/merged-agent-course/course-catalog.md`：唯一教材目录，维护 20 章入口、Review 状态、内容边界和旧稿说明。
- `docs/source-coverage.md`：唯一来源覆盖说明，维护 9 个来源与 20 章的映射、合并规则、排除项、缺口和审计结论。
- `source/`：原始教程材料。

## 工作原则

1. 不要遗漏 `source/` 下 9 份教程。
2. 同一主题出现在多个教程中时，应合并学习，而不是重复排课。
3. 中文版本优先；英文、日文版本主要作为补充或校对。
4. 不要随意修改 `source/` 下的原始教程，除非用户明确要求。
5. 新增学习笔记、总结、计划、设计文档时，优先放在 `docs/` 下。
6. 所有结论尽量附带来源路径，方便回溯。
7. 涉及实现时，先看相关教程代码，再写本项目自己的最小实现。
8. 修改多个文件后要复查内容一致性，并按“文档同步规则”更新相关入口和说明。

## 学习路线约定

默认按 `docs/agent-learning-guide.md` 的五个阶段推进：

1. 第 1-3 章：建立最小 Agent。
2. 第 4-8 章：建立稳定的认知运行时。
3. 第 9-12 章：加入框架、治理与扩展能力。
4. 第 13-16 章：走向常驻运行和多 Agent。
5. 第 17-20 章：评测、进化与生产化。

如果用户提出新的学习任务，应先判断它属于哪一阶段，再补充对应笔记或实验。

## 文档同步规则

三个核心文档各自只有一个职责，不复制彼此的详细内容：

- `agent-learning-guide.md` 回答“如何学习”。只有学习阶段、学习动作、实践产物或完成标准变化时才修改。
- `course-catalog.md` 回答“教材有哪些章节、当前状态如何”。章节增删、改名、排序或用户确认 Review 状态时必须修改。
- `source-coverage.md` 回答“内容来自哪里、是否覆盖”。来源增删、主题边界、章节落点、排除项或覆盖结论变化时必须修改。

发生以下变化时同步处理：

1. 新增、删除或重命名章节：更新 `course-catalog.md`；如果阶段归属或来源落点变化，同时更新学习指南和来源覆盖说明。
2. 修改章节正文：通常只修改章节；只有学习目标、来源覆盖或 Review 状态变化时才更新对应说明文件。
3. 新增或删除来源工程：更新 `README.md` 的项目描述、`agent-learning-guide.md` 的适用范围、`source-coverage.md` 和本文件。
4. 移动或重命名核心文档：更新 `README.md`、本文件以及仓库内全部引用，并检查 Markdown 本地链接。
5. Review 状态只有在用户明确确认后才能改为“已 Review”；状态只写在 `course-catalog.md`，其他文件通过链接引用，不复制状态表。

## 文档规范

- Markdown 文档使用清晰标题层级。
- 文件名使用小写英文和连字符，例如 `agent-learning-guide.md`。
- 结论型文档应包含：目标、来源、合并后的结论、待验证问题。
- 学习笔记建议按主题命名，不按教程目录机械拆分。
- 不复制大段原文，优先用自己的话总结。

## 实验规范

- 本项目自己的实验代码不要写入 `source/`。
- 后续如新增实验，建议放在 `experiments/` 或 `src/`。
- 每个实验应有 README 或文档说明：目标、运行方式、对应教程来源、验证结果。
- 需要密钥、模型、外部服务时，使用 `.env.example` 说明变量，不提交真实密钥。

## Codex 协作规范

- 开始任务前先快速查看相关文档和源材料，不凭记忆改结论。
- 文件搜索优先使用 `rg` 或 `find`。
- 手动编辑文件优先使用补丁方式。
- 不覆盖用户已有内容；如果发现已有同名文档，先读取再增量修改。
- 输出最终结果时，用简短中文说明新增或修改了哪些文件。

## 当前阶段

当前已经完成：

- 梳理 9 份教程目录。
- 建立五阶段学习指南和新版 20 章主学习路径。
- 合并来源覆盖地图与覆盖审计，建立统一来源覆盖说明。
- 生成新版融合教材第 1-20 章完整正文；具体 Review 状态只在 `docs/merged-agent-course/course-catalog.md` 中维护。
- 基于三篇 X 文章及公开官方资料完成新版第 15 章 `Loop Engineering：从单次循环到可持续执行系统`。
- 用户登录 X 后已逐篇读取三篇原文，并据此校准 14 步路线、五阶段 Loop、轻量生活 Loop 和停止/上下文/工具/验证四个难点。
- 已将 Loop Engineering 重构为主线教材式文章，并补充人的责任闭环及两张手绘风原创技术图；配图同时提供 SVG 与 Draw.io 可编辑格式。新版第 15 章继续复用 `assets/ch11/` 中的图源，旧 `ch11-loop-engineering.md` 仅作重构前版本留存。
- 已将 Anthropic 官方 Loop 四分类及其微信公众号中文整理中的新增视角并入新版第 15 章，包括控制权移交、Routine/Task 双层生命周期和成本运行策略。
- 将 `source/hello-agents` 的内容并入学习指南、来源覆盖说明和融合教材。

下一步建议：

- 从教材目录中第一章“待 Review”的章节继续逐章 Review、勘误和深度补充；只有用户确认后才修改目录状态。
- 20 章全部 Review 后，再决定是否把旧版第 2-14 章文件迁入 `legacy/`。
- 教材确认到可实践阶段后，按第 20 章选择综合项目方向并实现最小 Agent Loop 实验。
